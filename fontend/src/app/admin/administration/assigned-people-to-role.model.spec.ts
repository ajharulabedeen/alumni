import { AssignedPeopleToRole } from './assigned-people-to-role.model';

describe('AssignedPeopleToRole', () => {
  it('should create an instance', () => {
    expect(new AssignedPeopleToRole()).toBeTruthy();
  });
});
